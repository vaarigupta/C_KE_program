#include <stdio.h>
#include <stdlib.h>

int main()
{
   int x, y;
   printf("enter the number x");
   scanf("%d",&x);
   y = pow(x,5) +  10*pow(x,4)+ 8*pow(x,3) + 4*x + 3;

   printf("%d", y);











}
