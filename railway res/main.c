#include <stdio.h>
#include <stdlib.h>
int ticket( int price);
int main()
{int i,j,k,x,age;
int cost;
    printf("NORTH CENTRAL RAILWAY\n");
    printf("enter the age");
    scanf("%d",&age);

    cost = ticket(price);
    if (x>1 && x<=10)
    {
        printf("cost of ticket=%d",cost);

    }
    else if(x >10 && x <=20 )
    {
        printf("cost of ticket=%d",cost);


    }
    else if  (x<50 && x>20)
    {
        printf("cost of ticket=%d",cost);

    }
    else
   {
       printf("cost of ticket=%d",cost);

    }

    return 0;
}
int ticket(int price)
{


    costprice= 400*age;
    {
        printf("your ticket price is:%d", costprice);
    }
    return costprice;
}
