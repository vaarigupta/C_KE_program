#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main()
{
    int x,y,answer;
    printf(" enter the number x and y");
    scanf("%d%d",&x,&y);
     answer= 2.5*log(x) - cos(30) + abs( pow(x,2) -pow(y,2)) + sqrt(2*x*y);


      printf("answer");
      return 0;






}
