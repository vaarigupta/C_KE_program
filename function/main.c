#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main()
{
    float a,x,b, answer;
    printf("enter values of a, x, b");
    scanf("%f%f%f",&a,&x,&b);
    answer =((a*b) + b)/((a*x)-b);
    printf("%f",answer);










    return 0;
}
