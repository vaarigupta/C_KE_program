#include <stdio.h>
#include <stdlib.h>
int add(int x, int y);
int main()
{ int x, y, z;
printf("enter the numbers");
scanf("%d%d",&x,&y);
add(x,y);
printf("the sum is %d",z);

 return 0;
}
int add(int x, int y)
{ int z,a;
    z=x+y;
return a;
}
